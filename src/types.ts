export type ButtonStyle = {
    buttonPosition?: google.maps.ControlPosition,
    mainMargin?: string;
    backgroundColor?: string,
    symbolColor?: string
    border?: string,
    borderRadius?: string,
    outline?: string,
    height?: string,
    boxShadow?: string,
    cursor?: string,
}

export type MarkerStyle = {
    clickable?: boolean,
    cursor?: string,
    draggable?: boolean,
    fillColor?: string,
    scale?: number,
    strokeWeight?: number,
    strokeColor?: string,
    optimized?: boolean,
}
